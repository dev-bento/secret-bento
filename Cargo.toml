[package]
name = "secret-bento"
version = "0.4.0"
edition = "2021"
description = "Local secret scanning reports for AI-assisted cleanup."
license = "MIT"
repository = "https://github.com/dev-bento/secret-bento"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
